import { Button } from '../../components';
import { PlusIcon } from '@heroicons/react/outline';

# Button

Buttons are to be used as control that triggers an action. The content of the button has to express the action of the button itself.

### Sizes

<CodeEditor dependencies={{Button}} code={`
  <Button size="sm">harish</Button>
  <Button size="md">harish</Button>
  <Button size="lg">harish</Button>
`}>
</CodeEditor>

### Variants


<CodeEditor dependencies={{Button}} code={`
 <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="warning">Warning</Button>
  <Button variant="super">Super</Button>
  <Button variant="danger">Danger</Button>
`}>
</CodeEditor>

### Outline

<CodeEditor dependencies={{Button}} code={`
  <Button outline variant="primary">
    Login
  </Button>
  <Button outline variant="secondary">
    Signup
  </Button>
  <Button outline variant="warning">
    Edit
  </Button>
  <Button outline variant="super">
    Follow
  </Button>
  <Button outline variant="danger">
    Delete
  </Button>
`}>
</CodeEditor>

### Button with Icon

<CodeEditor dependencies={{Button, PlusIcon}} code={`
  <Button size="sm" outline icon={<PlusIcon className="h-3 w-3" />}>
    Add recipient
  </Button>
`}>
</CodeEditor>